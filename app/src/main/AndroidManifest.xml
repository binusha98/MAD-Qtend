<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mad_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Attendance"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MAD_APP">
        <activity android:name=".RVAdapter1"></activity>
        <activity android:name=".RVActivity1" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" /> <!-- The API key for Google Maps-based APIs. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCkwNCTVAJ4cZZUNdLXvTK9xa6WKx0VIRI" />

        <activity android:name=".Search" />
        <activity android:name=".Mark_Attendance" />
        <activity android:name=".Delete_Attendance" />
        <activity android:name=".Update_Attendance" />
        <activity android:name=".RVActivity" />
        <activity
            android:name=".attendance"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>